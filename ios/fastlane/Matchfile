git_url(ENV.fetch("MATCH_GIT_URL"))
storage_mode("git")
type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier([ENV["APP_BUNDLE_ID"], "com.modu.reader.shareExtension"])
